import type { Metadata } from "next";

export const metadata: Metadata = { title: "Ajukan Kampanye" };

export default function NewCampaignLayout({ children }: { children: React.ReactNode }) {
  return children;
}
